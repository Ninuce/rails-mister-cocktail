<div class="container">
  <h1><%= @cocktail.name %></h1>
  <% @cocktail.doses.each do |dose| %>
 <p> <span><%= dose.ingredient.name %> - </span><span><%= dose.description %></span>
  <%= link_to "delete this dose", dose_path(dose), method: :delete %></p>
  <% end %>
   <%= cl_image_tag @cocktail.photo, height: 300 %>
  <p><%= link_to "Back", cocktails_path, class: "btn btn-primary" %><p>
  <p><%= link_to "Add new dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %><p>
</div>
